[package]
name = "blockchain-app"
version = "0.1.0"
edition = "2024"

[dependencies]
sails-rs = "0.8.1"
parity-scale-codec = { version = "3.6", features = ["derive"] }
scale-info = { version = "2.10", features = ["derive"] }